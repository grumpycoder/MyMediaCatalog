@model MyMediaCatalog.Domain.MediaStaffMember

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>MediaStaffMember</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Media.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Media.Title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.StaffMember.Id)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.StaffMember.Id)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
