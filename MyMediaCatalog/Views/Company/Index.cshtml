@model IEnumerable<MyMediaCatalog.Domain.Company>

@{
    ViewBag.Title = "Companies";
}
<style>
    .table-hover {
        cursor: pointer;
    }
</style>
<h2>@ViewBag.Title</h2>
<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-default" })
</p>

<div id="mediaList">
    @Html.Partial("_CompanyList", Model)
</div>
